---
"@kode-frontend/commitlint-config": patch
---

Bump peer dep `@commitlint/cli` to `^19 || ^20` and `conventional-changelog-conventionalcommits` to `^9`. Kept node engine range `>= 18`. No breaking API changes.
