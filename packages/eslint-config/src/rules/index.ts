import { TConfig } from '../types'

export const configRules: TConfig = {
  'recommended-react': {
    // TODO: add eslint rules
  },
  'recommended-react-native': {
    // TODO: add eslint rules
  },
}
